class Solution {
public:
    int maxArea(vector<int>& height) {
        int maxWater   = 0;
        int lowIndex = 0;
        int riIndex = height[height.size() - 1];
        while((rightIndex - leftIndex) >= 1) {
            maxWater = height[rightIndex] - 
        }
        
        return maxWater;
    }
};
